[package]
name = "backend"
version = "0.1.0"
edition = "2024"

[dependencies]
embedded-can = "0.4"
futures-timer = "3.0"
socketcan = { version = "3.4", features = ["tokio"] }
tokio = { version = "1", features = ["full"] }